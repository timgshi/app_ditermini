<!-- This is the BOOTSTRAP HEADER. CURERNTLY TURNED OFF-->
<% if false %> 
<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "ditermini", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home",    root_path %></li>
          <li><%= link_to "Help",    help_path %></li>
          
          <% if signed_in? %>
            <li><%= link_to "Users", users_path %></li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        
        </ul>
      </nav>
    </div>
  </div>
</header>
<% end %>

<!-- This is the JQUERY HEADER. CURRENTLY TURNED ON! -->
<header>
  <div data-role="header"> 
    <h3>Ditermini</h3> 
    <% if signed_in? %>
      <a class="ui-btn-right", href="<%= url_for(signout_path) %>" data-method="delete">Sign Out</a>
    <% else %>
      <a class="ui-btn-right", href="<%= url_for(signin_path) %>">Sign In</a>
    <% end %>
  </div> 
</header>