<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<script type="text/javascript">
	$("button").bind("click", function (event, ui) {
		console.log("button clicked");
		var photoId;
		var voteYes;
		var id = event.srcElement.id;
		var parent = event.parentNode;
		if (id.substring(0, 2) == "no") {
			photoId = id.substring(2);
			voteYes = 0;
		} else {
			photoId = id.substring(3);
			voteYes = 1;
		}
		$.post("user/vote", {"photoId": photoId, "voteYes": voteYes}, function (data) {
			console.log(data);
			console.log($("#"+id).parent().html());
			$("#yes"+data.photo_id).html(data.yes);
			$("#no"+data.photo_id).html(data.no);
			console.log($("#yes"+data.photo_id).html());
			console.log($("#no"+data.photo_id).html());
			// $("#"+id).parent().parent().parent().html(data);
		});
	});
</script>
<% end %>